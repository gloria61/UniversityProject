<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android.birdsbees">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".views.SettingsView"></activity>
        <activity android:name=".views.ChatView" />
        <activity android:name=".views.AllChatsView" />
        <activity android:name=".views.HomeView" />
        <activity android:name=".views.LoginView" />
        <activity android:name=".views.SurveyFourthView" />
        <activity android:name=".views.SurveyThirdView" />
        <activity android:name=".views.SurveySecondView" />
        <activity android:name=".views.SurveyFirstView" />
        <activity android:name=".views.RegisterView" />
        <activity android:name=".views.WelcomeView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>